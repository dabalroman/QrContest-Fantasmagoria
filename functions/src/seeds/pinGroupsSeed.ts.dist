import { PinGroup } from '../types/pinGroup';

// TEMPLATE - copy to pinGroupsSeed.ts and declare the real taxonomy there. The real file is
// gitignored and ships in seeds.zip.
//
// Keep the real list a SUPERSET of every `groups[]` value used in pinsSeed.ts: upsertPinHandle
// validates a pin's groups against this collection, and a group that exists only on a pin can never
// back a `pinsInScope` achievement scope (`group:<uid>`).
const pinGroupsSeed: PinGroup[] = [
    { uid: 'przyklad', name: 'Przykład' }
    // ...
];

export default pinGroupsSeed;
