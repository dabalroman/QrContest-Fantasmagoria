import { CardClue } from '../types/cardClue';
import { CardTier } from '../types/card';

// TEMPLATE - copy to cluesSeed.ts if the card game is ever revived. RETIRED for 2026: nothing imports
// this seed and seedDatabaseHandle no longer writes the `clues` collection, so on a fresh database it
// stays empty. The real file is gitignored and ships in seeds.zip; the clue text is a spoiler (each
// entry riddles the location of a hidden mythical/legendary card), so nothing real belongs here.
const cluesSeed: CardClue[] = [
    {
        uid: 'przykladowa-wskazowka',
        order: 1,
        image: null,
        title: 'Tytuł wskazówki',
        description: 'Treść wskazówki prowadzącej do ukrytej karty.',
        tier: CardTier.LEGENDARY
    }
    // ...
];

export default cluesSeed;
